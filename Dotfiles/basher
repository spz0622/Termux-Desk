
# Enter Your Username Here
# Hostname can Be Your Desk or Device Name..

export USER=Termuser
export HOSTNAME=Term-Desk


echo "How Are You Today, $USER"
lsd
echo ""

alias ls="lsd"
alias la="lsd -a"
alias rf="rm -rf"
alias extr="tar xf"
alias compr="tar -cvf"
alias i="apt install"
alias rem="apt remove"
alias sd="/sdcard"
alias home="$HOME"
alias used="ncdu"
alias n="nnn"
alias append="echo >>"
alias play="mpv"

# Symlink Fixes
rm -rf ~/DCIM/* ~/Android/* && ln -s ~/storage/dcim/* ~/DCIM && ln -s ~/storage/shared/* ~/Android